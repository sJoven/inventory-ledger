//use isLoggedIn.ts
//using the session.userid, look for the .is_client field
//this is an array of store_id(string)
//display all stores as cards with (storeid, storeName) information
//for each card, have a link going to client/{store_id}

//components: cards

const page = () => {
  return <div>a</div>;
};

export default page;
