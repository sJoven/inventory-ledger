//use isLoggedIn.ts and access.ts' adminAccess function
//the return from access.ts is an array of objects(store_id, store_name and role)
//display all stores as cards with all 3 information
//for each card, have a link going to admin/{store_id}

//components: cards, invite cards
