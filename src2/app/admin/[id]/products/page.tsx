//get [id] params
//user isLoggedIn.ts as well as the canShowAdmin function of canUser.ts
//pass parameters: canShowAdmin(id, permission);
//showProducts
//get [id], [page] and [query] params

//function pageNum
//count = count all products in store [id]
//return pageNum = count / 10

//whereClause
//use pageNum = const pageNum
//in store_id = [id], get all is_deleted false
//and (name or sku contains [query])
//select: id, name, sku, quantity
//take: 10
//const skip = ([page] - 1) * 10;

//display name, sku and quantity with id as the row's key

//components
//createbtn
//create modal
//action btn
//action selection (edit, delete)
//edit modal
//delete modal
//pagination
